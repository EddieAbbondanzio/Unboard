/**
 * A component that represents some kind of form. Offers
 * the ability to perform validation, and more.
 */
export abstract class FormComponent {
    /**
     * Submit the form.
     */
    submit(): void;
}